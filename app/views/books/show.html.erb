   <% if flash[:notice] %>
     <p id="notice">
       <%= flash[:notice] %>
     </p>
   <% end %>

<h4>Title</h4>
  <%= @book.title %>

<h4>Body</h4>
  <%= @book.body %>

<div>
  <%= link_to 'Edit', edit_book_path(params[:id]) %>|<%= link_to 'Back', books_path %>
</div>